---
description: WannaBuild condensed workflow contract
globs:
  - "**/*"
alwaysApply: false
---

Use WannaBuild as a condensed workflow:

1. Discover
2. Control mode gate
3. Optional research gate
4. Plan
5. Implement
6. Review
7. QA
8. Summary

Key rules:

- Treat `AGENTS.md` as the main operator contract.
- Treat `skills/build/SKILL.md` as the orchestrator spec.
- Use `.wannabuild/` artifacts as the workflow state source of truth.
- Create an isolated workspace before real work in a git repo.
- After Discover, ask once whether to stay guided or switch to autonomous mode.
- Keep Discover, Plan, QA, and Summary single-lane by default.
- Use bounded multi-agent research only when it materially improves planning quality.
- Use parallelism mainly for disjoint implementation slices and reviewer hats.
- The integration tester is the hard gate.

Before phase transitions, run:

```bash
scripts/validate-wannabuild-artifacts.sh <target_project> <target_phase>
```

For repo readiness, run:

```bash
scripts/wannabuild-doctor.sh
```
