---
description: Prepare the WannaBuild final handoff
argument-hint: [scope]
---

# /wb-ship

Use the `wb-ship` skill for ship-focused work.

Contracts live in the owning skill; this command is only a shortcut.
